README for packet analyzer
@author = Jian He

To run the application:
1, unzip pktanalyzer file
2, open terminal
3, go to the directory where unzipped files located
4, compile java files with command "javac *.java" if they are not compiled
5, use command java pktanalyzer binary_file_path to see result